<div class="signers">
    <h1>who else signed my petition?</h1>
    <div class="container">
        <div>
            {{#rows}}
                <h2>{{first}} {{last}}</h2>
                <p>at {{created}}</p>
            {{/rows}}   
        </div>
    </div>
</div>
